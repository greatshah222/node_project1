const p = {
    name: 'Bishal',
    cast: 'shah',
    favourit: [1, 2, 'age', 'hello']
};


let output;
output = p.name.replace('Bishal', 'whah');
output = output.replace('shah', 'bishlu')

console.log(output);
